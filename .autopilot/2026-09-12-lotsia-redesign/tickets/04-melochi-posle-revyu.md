# 04 — Мелочи после ревью

**Требования:** R16, R23, R26 (разбор отложенных находок ревью, `phases/8-final.md` §1a)
**Blocked by:** 01, 02, 03
**Зона:** `assets/style.css` · `order-check/` · `grand-total/app.js`
**Волна:** 3
**Status:** ready

## Что должно заработать

Четыре точечных правки, найденные ревьюерами после тасков 01–03. Ничего нового,
только доводка того, что уже построено.

## Из брифа, дословно

> «Итог — цифрами и сразу. После обработки сначала главное — строки, контейнеры, вес, — а рядом кнопка «Скачать».»
> «Логика инструментов не меняется.»

## Критерии приёмки

- [ ] Блок предупреждений (`.warning-message`) и блок ошибки (`.error-message`) на страницах `.app`: у вложенных `<p>`/`<ul>` нет стандартных браузерных отступов, из-за которых блок выше, чем нужно, — одно общее правило в `assets/style.css`, работает на всех страницах `.app`
- [ ] «Сверить с поручениями»: если размер файла из архива неизвестен, в строке списка нет пустой ячейки `.file-list__meta` (не выводится или говорит что-то осмысленное, например «из архива»)
- [ ] «Сверить с поручениями»: ошибки нескольких архивов показываются каждая отдельной строкой (или пунктом списка), а не склеенными в одну строку
- [ ] `assets/style.css`: удалены правила, дублирующие глобальные `[hidden]` и `.mono` (`.home-panel[hidden]`, `.data-table .mono` или аналогичные), вид страниц не изменился
- [ ] `grand-total/app.js`: у плитки «Коносаментов» — однострочный комментарий, что строка «Feeder Bill of Lading - N sets» под таблицей — предпросмотр той же строки PDF/Excel и не дубль (чтобы её не удалили)
- [ ] `?v=` обновлён у каждого изменённого файла по цепочке, на всех страницах, где он подключён (`assets/style.css` — все 9 страниц)
- [ ] `npm test` зелёный целиком; `core.js` не меняются
