# Манифест требований

Источник: `2026-09-12-brief.md`. Строку из этого списка может снять **только пользователь**.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «Необходимо создать еще один инструмент» / «создай инструмент проверки» | done | коммит `3aba5d6`; подтверждено вживую на дев-сервере | spec: истории 1, 25; §1, §8 |
| R02 | «в экспорте» | done | коммит `3aba5d6`: `category: 'export'` | spec: история 1 |
| R03 | «Он должен быть самым первым в списке инструментов» | done | коммит `3aba5d6`; подтверждено вживую: первый в панели, «маршруте» и карточках вкладки «Экспорт» | spec: история 1 |
| R04 | «Этот инструмент должен сверять данные которые прописали в шаблоне с данными в поручении» | done | коммит `123b5a6`; сверка по контейнеру подтверждена на обеих настоящих парах (пара 1 — 0 расхождений, пара 2 — только согласованные предупреждения). **Частично отложено** (spec «Вне рамок»): наименование груза рус., терминалы E5/E6/F и адреса Q/S/U с поручением не сравниваются — в верных образцах они законно отличаются от поручения; X, Y не проверяются — в отчёте Phase 8, «что не вошло» | spec: истории 13, 16; §5, §5.4; «Вне рамок» |
| R05 | «также обязательно должна быть сверка соответствия шаблону по форме» | done | коммит `123b5a6`; форма выведена из двух образцов (`reference.md`) | spec: истории 7–12; §3, §4 |
| R06 | «и названия листа» | done | коммит `123b5a6`: лист `EXPORT_MANIFEST` | spec: история 6; §4.1 |
| R07 | «иначе не получиться его загрузить в систему» — проверка формы должна ловить ровно то, из-за чего система не примет файл | done | коммит `123b5a6`; эталон формы — образцы, которые система приняла (правил самой системы пользователь не описывал) | spec: истории 6–12, 23; §3, §4 |
| R08 | «Проанализируй полученные файлы» — правила проверки выводятся из присланных образцов | done | коммит `123b5a6`; разбор — `reference.md` | spec: история 28; §3, §5 |
| R09 | «Они оба заполнены корректно» — оба образца должны проходить проверку как верные | done | коммит `123b5a6`/`3aba5d6`; подтверждено модулем и вживую в браузере на обеих настоящих парах: пара 1 — 0 ошибок/0 предупреждений, пара 2 — 0 ошибок/5 предупреждений (места GESU6511445 и четыре подозрительные даты — по G01/A01, не ошибки) | spec: история 28; §5, §5.3 |
| R10 | «Эти файлы приходят от заказчика» — на вход идут файлы в том виде, в каком их прислал заказчик: шаблон и поручение | done | коммит `3aba5d6`; настоящие пары поданы в зону как есть, роли распознаны верно | spec: истории 2, 3; §6, §8 |
| R11 | «на выходе должен быть шаблон с отмеченными желтым цветом неверными ячейками» | done | коммит `123b5a6`/`3aba5d6`; скачан вживую — жёлтая заливка и примечания на месте, остальное оформление не тронуто | spec: истории 18, 19; §5.5 |
| R12 | «а также текстовый документ с описанием ошибок» | done | коммит `123b5a6`/`3aba5d6`; скачан вживую — `.txt` c BOM и CRLF | spec: истории 20, 24; §7 |
| R13 | «Если есть возможность внести изменения без уточнения у заказчика» — ошибки делятся на исправимые без заказчика и требующие уточнения | done | коммит `123b5a6`. Исправимо без заказчика — форма (имя листа, заголовки, числа текстом) и значения из поручения (номер поручения, судно, рейс, ISO, пломбы, вес груза, вес тары, ВГМ ниже «груз + контейнер» → из поручения); не исправляется никогда — места (G01) и всё, чего в поручении нет. Ячейки A, C, E3, E4, G, J, K, N не меняются вовсе; исправляются из поручения B, D, E, I, L, M, O, Z, E1, E2; форма — лист, шапка, заголовки, лишние листы/колонки/пустые строки/«итого» (строго по §4.4, не шире — исправлено на ревью), объединения | spec: история 21; §4, §5 |
| R14 | «должна появляться кнопка по выгрузке исправленного шаблона» | done | коммит `123b5a6`/`3aba5d6`; подтверждено вживую на паре 1 с внесёнными правками — кнопка появилась первой видимой, подпись «Исправлено 2 из 2 ошибок — шаблон готов к загрузке» | spec: история 22; §8 |
| R15i | *(подразумевается)* «во вложении 2 архива. В каждом из них по 2 файла» — файлы приходят парой (шаблон + поручение), обычно вложениями одного письма; инструмент должен понимать, какой из файлов шаблон, а какой поручение | done | коммит `3aba5d6`; принимаются `.xlsx` и `.zip` («скачать все вложения» Gmail), роль файла определяется по содержимому — подтверждено вживую на обеих парах | spec: истории 2–5; §6, §8 |
| R16i | *(подразумевается)* исправленный шаблон сам должен проходить проверку формы — иначе его тоже не загрузить в систему (из R07 + R14) | done | коммит `123b5a6`; подтверждено свойством «повторная проверка исправленного даёт 0 находок исправлю сам» — тестами и на обеих настоящих парах с внесёнными правками | spec: история 23; §3 |
| R17i | *(подразумевается)* инструмент устроен как остальные на сайте: каркас «Лоции», шаги 1→2→3, файлы не покидают браузер, тесты в `npm test`, версии `?v=` | done | коммит `3aba5d6`; каскад `?v=` на всех десяти страницах, каркас и шаги как у остальных инструментов | spec: истории 25, 26; §1, §8, §9 |
| R18i | *(подразумевается)* остальные инструменты сайта продолжают работать как раньше (в том числе «Сверить с поручениями», читающий тот же формат поручения) | done | коммит `2761073`; `order-check/core.test.js` не изменён и зелёный, индекс сверки совпал со старым разбором на обеих настоящих парах (см. D01 — единственная разница в поведении на нестандартных файлах, принятая как исправление) | spec: история 27; §2; «Границы» |
| G01 | «Количество мест сверять но никогда не исправлять.» | done | коммит `123b5a6`; подтверждено тестами и на настоящей паре 2 (GESU6511445, предупреждение, ячейка K не меняется). Места сверяются всегда и не меняются никогда. Уровень выбран так, чтобы образец 2 остался верным (R09): один товар в контейнере — ошибка «уточнить у заказчика», несколько товаров — предупреждение «проверьте» (так было в рекомендованном варианте вопроса; пользователь ответил своими словами, не выбрав уровень — сказано в отчёте) | spec: история 15; §4.5; §5.2 (K) |
| D01 | *(доказано кодом, таск 01)* общий разбор поручения останавливается на «Дополнительные сведения» и ищет подписи во всех строках выше таблицы — у «Сверить с поручениями» на нестандартных файлах поведение изменилось | done | служит R18i: на правильных файлах результат прежний (проверено на обоих настоящих поручениях и неизменными тестами order-check); на нестандартных — исправлены два скрытых дефекта старого разбора (ложный контейнер из текста ниже «Дополнительные сведения», номер поручения в строках 6–7). Принято как исправление, spec §2 дополнен | spec §2 → T01 |
| G02 | «ВГМ больше либо равно вес груза+вес контейнера» | done | коммит `123b5a6`; подтверждено тестами и на настоящей паре 1 (три контейнера с ВГМ = сумме — не ошибка). Ошибка — только ВГМ меньше суммы веса груза и веса контейнера | spec: история 14; §5.2 (O) |
| G03 | «необходимо добавить проверку, чтобы все значения во всех строчках были одинаковыми (защита от искажения при протягивании данных в excel)» | done | коммит `8cf10e1`; правило A07 (проверка общих полей внутри одного коносамента) было предупреждением, стало ошибкой: жёлтая заливка, «уточнить у заказчика» (не auto — нельзя знать, какая из разошедшихся строк верна), находка на каждой расходящейся строке, не только на первой. Подтверждено тестами (8 полей, несколько расходящихся строк, несколько коносаментов в одном файле) и обеими настоящими парами — новых находок не появилось, как и должно | spec: §5.2 A07 (обновлено), §5.5 |
| G04 | «Ячейки с предупреждением комментарий стоит но никаким цветом не подкрашиваются, подкрась тоже, но голубым цветом» | done | коммит `6a31061`, сделано без формального таска (по прямой просьбе пользователя — «продолжи без автопилота»). Увидено на развёрнутом на VPS инструменте (пара gmail7): у предупреждений (даты, места) есть примечание, но нет заливки. Оттенок голубого — ремесло, не переспрашивал: взят стандартный «светло-голубой» `ADD8E6`, отличимый от жёлтого ошибок; ячейка с ошибкой и предупреждением одновременно — жёлтая. Подтверждено обновлённым тестом, байтовой проверкой обеих настоящих пар и вживую в браузере — числа не изменились, все 5 предупреждений пары gmail7 голубые | spec: §5.5 (обновлено) |
| G05 | «сделай еще одну проверку терминал ТЕРМИНАЛ ВЫГРУЗКИ в шапке шаблона должен совпадать с портом выгрузки. Это можно исправлять самостоятельно. Терминал выгрузки который в таблице не должен при этом исправляться.» | done | сделано без формального таска. На обоих настоящих образцах E6 («El Dekheila Alexandria Int. Container Terminal») не совпадает буквально с «Порт выгрузки» поручения («El Dekheila(EGEDK)») — при точном сравнении текста НОВОЕ правило сломало бы R09 (оба образца должны проходить без ошибок). Проверено побайтово, затем решено ремеслом: сравнение не текстом целиком, а названием порта без скобочного кода, вхождением в любую сторону (`samePort`/`stripPortCode`) — на обоих образцах находки нет, как и раньше. Как СУДНО/РЕЙС: если известно одно значение по всем поручениям и оно не совпадает — «исправлю сам»; если поручения разошлись между собой — «уточнить у заказчика». Столбец F (ТЕРМИНАЛ ВЫГРУЗКИ (коносамент), построчно) по-прежнему не сравнивается с поручением — так и было решено в spec «Вне рамок», пользователь это подтвердил явно | spec: §5.1 (обновлено) |
| G06 | «в файле с самостоятельными исправлениями должны остаться цветовые выделения оставшихся замечаний и ошибок с комментариями» | done | сделано без формального таска. Исправленный шаблон раньше строился с нуля по эталонной форме и терял всякую заливку/примечания; теперь `buildCorrectedWorkbook` строит карту «адрес на исходном листе → адрес той же ячейки в исправленном» по ходу переноса значений (не по буквальному совпадению адресов — пустые/итоговые строки убраны, лишние/недостающие колонки убраны/добавлены, поэтому адрес мог сдвинуться) и красит на исправленном шаблоне все находки, которые НЕ `fix:'auto'` (то, что осталось незакрытым): ошибки жёлтым, предупреждения голубым (G04), с тем же примечанием. Auto-исправленные ячейки остаются чистыми. Подтверждено тестом на сдвинутой строке (пустая строка выше — данные переносятся на другой номер строки, и находка красится именно там, не на старом адресе) и на настоящем файле с внесённой порчей | spec: §5.5 (обновлено) |
