# Манифест требований

Источник: `2026-09-06-brief.md`. Строку из этого списка может снять **только пользователь**.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «сделать еще один инструмент» | in-spec | — | Истории 1, 20 |
| R02 | «при загрузке большого файла содержанием манифестов» | in-spec | — | Истории 2–5 |
| R03 | «используя алгоритмы подсчета общих весов, которые у нас используются в инструментах по портам отправления и прибытия» | in-spec | — | Истории 6–8, `lib/port-breakdown.js` |
| R04 | «Сформировать итоговый файл в формате PDF» | in-spec | — | Истории 9–11 |
| R05 | «Позывной... судна Тебе взять неоткуда, поэтому сделай так, чтобы эти поля... заполнялись вручную» (позывной) | in-spec | — | Истории 12, 15 |
| R06 | «дату прибытия судна Тебе взять неоткуда, поэтому сделай так, чтобы эти поля... заполнялись вручную» (дата прибытия) | in-spec | — | Истории 13, 15 |
| R07i | *(подразумевается)* содержание PDF — разбивка итогов по типам контейнеров (кол-во/вес груза/тары/всего) плюс общая сумма, по образцу приложенного пользователем файла `GRAND_TOTAL_aligned (1).pdf` | in-spec | — | Истории 16–17 |
| R08i | *(подразумевается)* название судна и номер рейса — не «взять неоткуда» (в отличие от R05/R06), значит подставляются автоматически из шапки манифеста | in-spec | — | Истории 18–19, `readVoyageHeader` |
| R09i | *(подразумевается)* некорректный/несовместимый файл — понятная ошибка, как во всех остальных Excel-инструментах сайта | in-spec | — | Истории 4–5 |
| R10i | *(подразумевается)* новый инструмент отображается в списке на главной странице | in-spec | — | История 20 |
| G01 | «Ручной ввод, как позывной и дата» (ответ на брифинг про поле TERMINAL) | in-spec | брифинг 2026-09-06: терминал в манифесте не встречается ни в одной колонке, пользователь выбрал ручной ввод, как у позывного и даты | История 14, 15 |
| D01 | *(обнаружено при сборке)* Стандартные 14 шрифтов PDF (`StandardFonts.Helvetica`) — WinAnsi-кодировка, кириллицы в ней нет: `page.drawText('ВЫГРУЗКА ...')` бросает исключение прямо внутри `pdf-lib`. Проверено в браузере на реальном файле. | in-spec | Поправлено: шрифт грузится с постоянного версионированного URL Google Fonts и встраивается через `@pdf-lib/fontkit` (`subset: true`). Второй найденный по ходу вариант — Google Fonts `text=`-сабсеттинг (woff2) — с той же версией `pdf-lib`/`fontkit` либо зависает на `doc.save()` без ошибки, либо (без сабсеттинга) даёт неверную раскладку глифов; обычный статический `.ttf` обеих проблем не имеет | spec «Решения по реализации» |

## Ссылки

- Пример конечного результата, приложенный пользователем: `/Users/nasekomus/Downloads/GRAND_TOTAL_aligned (1).pdf` — сохранён как эталон в `reference.md`.
- Пример исходного манифеста (тот же рейс — VESSEL «GUANG QI DE ER TA», VOYAGE «2601W»): `/Users/nasekomus/Downloads/Gmail (2)/+manifest_TOTAL_GQDE_2601W.xlsx`.

### Statuses

| Status | Meaning |
|---|---|
| `open` | not yet resolved |
| `in-spec` | landed in the spec |
| `in-ticket` | a ticket exists |
| `done` | built and reviewed |
| `placeholder` | stub where a user fact belongs |
| `deferred` | consciously postponed |
| `dropped` | cancelled by the user |
