# Манифест требований

Источник: `2026-09-06-brief.md`. Строку из этого списка может снять **только пользователь**.

Собрано одним проходом (ярус T0), коммит `dda6163`.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «сделать еще один инструмент» | done | — | Истории 1, 20 |
| R02 | «при загрузке большого файла содержанием манифестов» | done | — | Истории 2–5 |
| R03 | «используя алгоритмы подсчета общих весов, которые у нас используются в инструментах по портам отправления и прибытия» | done | — | Истории 6–8, `lib/port-breakdown.js` |
| R04 | «Сформировать итоговый файл в формате PDF» | done | — | Истории 9–11 |
| R05 | «Позывной... судна Тебе взять неоткуда, поэтому сделай так, чтобы эти поля... заполнялись вручную» (позывной) | done | — | Истории 12, 15 |
| R06 | «дату прибытия судна Тебе взять неоткуда, поэтому сделай так, чтобы эти поля... заполнялись вручную» (дата прибытия) | done | — | Истории 13, 15 |
| R07i | *(подразумевается)* содержание PDF — разбивка итогов по типам контейнеров (кол-во/вес груза/тары/всего) плюс общая сумма, по образцу приложенного пользователем файла `GRAND_TOTAL_aligned (1).pdf` | done | — | Истории 16–17 |
| R08i | *(подразумевается)* название судна и номер рейса — не «взять неоткуда» (в отличие от R05/R06), значит подставляются автоматически из шапки манифеста | done | — | Истории 18–19, `readVoyageHeader` |
| R09i | *(подразумевается)* некорректный/несовместимый файл — понятная ошибка, как во всех остальных Excel-инструментах сайта | done | — | Истории 4–5 |
| R10i | *(подразумевается)* новый инструмент отображается в списке на главной странице | done | — | История 20 |
| G01 | «Ручной ввод, как позывной и дата» (ответ на брифинг про поле TERMINAL) | done | брифинг 2026-09-06: терминал в манифесте не встречается ни в одной колонке, пользователь выбрал ручной ввод, как у позывного и даты | История 14, 15 |
| D01 | *(обнаружено при сборке)* Стандартные 14 шрифтов PDF (`StandardFonts.Helvetica`) — WinAnsi-кодировка, кириллицы в ней нет: `page.drawText('ВЫГРУЗКА ...')` бросает исключение прямо внутри `pdf-lib`. Проверено в браузере на реальном файле. | done | Поправлено: шрифт грузится с постоянного версионированного URL Google Fonts и встраивается через `@pdf-lib/fontkit` (`subset: true`). Второй найденный по ходу вариант — Google Fonts `text=`-сабсеттинг (woff2) — с той же версией `pdf-lib`/`fontkit` либо зависает на `doc.save()` без ошибки, либо (без сабсеттинга) даёт неверную раскладку глифов; обычный статический `.ttf` обеих проблем не имеет | spec «Решения по реализации» |
| G02 | «посмотри внимательно на форматирование файла ПДФ, который я тебе дал как образец. Сохрани форматирование как в исходном файле в пдф» | done | пользователь 2026-09-06: PDF собирался по памяти о макете, не по внимательному разбору эталона — построчная линия под каждой строкой таблицы, отсутствие точного подбора ширины колонок под заголовки. Проверено записью реальных координат отрисовки (drawText/drawLine) при живой генерации — линия под шапкой таблицы, между строками данных линий нет, линия перед `TOTAL:` и под ней | spec «Дополнение — точное соответствие формату эталона» |
| G03 | «также сделай дополнительно выгрузку аналогичного файла но уже в excel с форматированием как в образце» | done | пользователь 2026-09-06: вторая, отдельная кнопка скачивания — .xlsx с тем же составом и оформлением (не замена PDF, а вдобавок к нему). Проверено пересборкой книги обратно через ExcelJS — веса лежат числами (не текстом) с форматом ячейки `#,##0.000" KGS"`, границы и жирность на месте | spec «Дополнение — экспорт в Excel» |
| D02 | *(обнаружено при сборке)* Первая версия `buildExcelWorkbook` переиспользовала `buildTableRows` — те же строки, что и PDF/предпросмотр, где вес уже отформатирован в текст («8 692 506,220 KGS»). `numFmt`, поставленный на такую ячейку, не имеет эффекта: Excel хранит и показывает её как текст, а не число. Обнаружено пересборкой файла обратно через `ExcelJS` сразу после первой реализации, до показа пользователю. | done | Заведена отдельная `buildExcelRows` — те же данные, но веса передаются числами (`entry.cargoWeight` и т. д.), текстовый вид даёт только `numFmt` ячейки. Ширина колонок при этом всё равно считается по отображаемому тексту (число, прогнанное через тот же `formatWeight`), а не по длине самого числа | `grand-total/app.js` |

## Ссылки

- Пример конечного результата, приложенный пользователем: `/Users/nasekomus/Downloads/GRAND_TOTAL_aligned (1).pdf` — сохранён как эталон в `reference.md`.
- Пример исходного манифеста (тот же рейс — VESSEL «GUANG QI DE ER TA», VOYAGE «2601W»): `/Users/nasekomus/Downloads/Gmail (2)/+manifest_TOTAL_GQDE_2601W.xlsx`.

### Statuses

| Status | Meaning |
|---|---|
| `open` | not yet resolved |
| `in-spec` | landed in the spec |
| `in-ticket` | a ticket exists |
| `done` | built and reviewed |
| `placeholder` | stub where a user fact belongs |
| `deferred` | consciously postponed |
| `dropped` | cancelled by the user |
