# Манифест требований

Источник: `2026-09-06-brief.md`. Строку из этого списка может снять **только пользователь**.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «добавить еще 1 инструмент по формированию дашборда по информации из файла» | done | `dashboard/` — карточка, страница, логика, тесты; коммит `7c3b842` | spec §Истории 1, 9–11 |
| R02 | «по каждому порту назначения получить разбивку» | done | проверено на реальном файле-примере — 2 порта | spec §Истории 2, 13 |
| R03 | «разбивку по типам контейнеров» | done | проверено на реальном файле — 6 типов | spec §История 3 |
| R04 | «и их весовым характеристикам» | done | брифинг: количество контейнеров + суммарный вес груза/тары/общий по группе «порт × тип» | spec §История 4 |
| R05i | *(подразумевается)* входной файл — манифест в том же формате, что уже читают «Склейка» и «Сверка ОГ» (лист «Manifest», шапка C:Y) | done | переиспользуем `lib/manifest-format.js` (`validateStructure`), как остальные два инструмента | spec §Решения — формат входа |
| R06i | *(подразумевается)* новый инструмент — отдельная страница сайта, карточка на главной (`tools.js`), общий стиль (`assets/style.css`) | done | по образцу `merge/` и `dg-check/` | spec §Решения — модуль |
| R07i | *(подразумевается)* какой именно вид у «весовых характеристик» | done | брифинг: количество + суммы, без среднего/мин/макс — см. R04 | spec §История 4 |
| R08i | *(подразумевается)* формат самого дашборда | done | таблицы на странице проверены в браузере на реальном файле; экспорт в Excel проверен round-trip'ом | spec §История 8 |
| R09i | *(подразумевается)* один входной файл за раз или несколько | done | заменено ответом брифинга — см. G01, G02 | spec §Решения — `perFile`/`combined` |
| R10i | *(обнаружено при разборе примера файла)* один и тот же порт назначения в реальных данных встречается написанным по-разному («St.Petersburg, Timber Port (RUPLP)» и «St.Petersburg, Timber Port(RUPLP)» — без пробела перед скобкой) — группировка должна считать это одним портом | done | нормализация убирает именно пробел перед «(» (уточнено против первой формулировки — см. interfaces.md); проверено на реальном файле — оба варианта схлопнулись в один порт | spec §История 13 |
| G01 | «несколько файлов но тогда своды по каждому файлу отдельно» | done | `perFile`, отдельный блок на странице на каждый файл | spec §Истории 5–6 |
| G02 | «и итоговую сводную таблицу» | done | `combined`, блок «Итого по всем файлам» | spec §История 7 |
| D01 | *(доказано сборкой)* прислан более полный реальный файл рейса + эталонная сводная таблица пользователя — «Кол-во» изначально считало строки манифеста, а не контейнеры (один контейнер с несколькими видами груза занимает несколько строк), и итоговая строка-«СУММ()» внизу листа реального файла удваивала общий вес | done | коммит `b2a840a` — «Кол-во» теперь считает уникальные номера контейнеров; строка без номера контейнера не считается данными. Проверено: вес и количество контейнеров сошлись с эталонной таблицей пользователя до копейки/контейнера | spec §D01 |

## Дополнения

- 2026-09-06 — прислал файл `+manifest_TOTAL_GQDE_2601W.xlsx` (более полный реальный манифест того же рейса) и эталонную сводную таблицу `Total.xlsx`, попросил сверить: «во вложении файл и результаты которые должны быть на выходе. Проверь сходится ли. В этих файлах все верно»

