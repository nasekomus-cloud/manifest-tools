# Изначальная задача

> Записано 2026-09-06. Текст задачи не редактируется — он эталон, с которым
> сверяется готовый результат. Всё, что сказано позже, дописывается
> в «Дополнения» ниже.

Посмотри мой последний чат. Хочу сделать сайт с минимальными компонентами автоматизации для моей жены. Два необходимые действия уже учтимы в этом чате. Подумай и сделай сайт, который мы в дальнейшем будем наполнять дополнительными данными. /autopilot

## Дополнения

- 2026-09-06 — пользователь прислал содержимое упомянутого «последнего чата» (переписки о работе с судовыми манифестами), дословно:

  > @"/Users/nasekomus/Downloads/Gmail/+man_EGEDK.xlsx" @"/Users/nasekomus/Downloads/Gmail/+man_NICT.xlsx" @"/Users/nasekomus/Downloads/Gmail/+man_NNBO.xlsx" @"/Users/nasekomus/Downloads/Gmail/+man_PK.xlsx" @"/Users/nasekomus/Downloads/Gmail/+man_Shanghai.xlsx" @"/Users/nasekomus/Downloads/re/GUANG QI DE ER TA 2601W DG MANIFEST.xlsx" @"/Users/nasekomus/Downloads/manifest_combined (2).xlsx"
  > /autopilot
  > +man_CNQTAO.xlsx
  > +man_CNQTAO.xlsx
  > XLSX
  > +man_EGEDK.xlsx
  > +man_EGEDK.xlsx
  > XLSX
  > +man_NICT.xlsx
  > +man_NICT.xlsx
  > XLSX
  > +man_NNBO.xlsx
  > +man_NNBO.xlsx
  > XLSX
  > +man_PK.xlsx
  > +man_PK.xlsx
  > XLSX
  > +man_Shanghai.xlsx
  > +man_Shanghai.xlsx
  > XLSX
  > проверь структуру файлов. Они должны быть одинаковыми. Сделай из итих файлов один общий файл. форматы и выделения ячеек сохраняй
  > Viewed a file, ran a command
  > Все файлы имеют один лист "Manifest" и одинаковую сетку колонок (C:Y), но разное число строк. Посмотрю подробнее на заголовки и структуру, чтобы убедиться, что колонки реально совпадают.
  > Ran 15 commands, edited a file, read a file
  > Проверил структуру всех 6 файлов — она полностью одинаковая:
  >
  > - Один лист "Manifest", шапка в строках 3–5 (одна и та же для всех — рейс т/х GUANG QI DE ER TA, №2601W), данные начинаются с строки 6, колонки C:Y (23 поля).
  > - Заголовки колонок идентичны во всех файлах.
  > - Выделения заливкой (fill) в данных нигде не используются — только рамки (thin по строкам данных, medium по шапке) и стандартный шрифт Calibri 10.
  >
  > Собрал их в один файл в порядке: CNQTAO → EGEDK → NICT → NNBO → PK → Shanghai (242+92+56+83+60+382 = 915 строк данных). Для каждой строки скопированы значения и полный стиль (шрифт, заливка, рамки, формат чисел, выравнивание) из исходной ячейки, шапка и ширины столбцов взяты из первого файла.
  > Важный момент: колонку «№п/п» я не пересчитывал — оставил как есть в каждом файле, поэтому нумерация повторяется по разделам (например, снова начинается с 1 в блоке EGEDK). Если нужна сквозная нумерация по всему своду — скажите, пересчитаю в одну секунду.
  > Manifest combined
  > Spreadsheet·XLSX
  > предложи как можно автоматизировать данную операцию оптимальным образом
  > Thought for 27s
  > Расскажу по вариантам, а дальше уточню, что вам удобнее внедрить.
  > Скрипт уже написан наполовину — то, что я сделал выше, легко превратить в универсальный инструмент. Ключевые доработки для "оптимальности":
  >
  > 1. Автопоиск файлов — вместо жёстко прописанного списка, скрипт сканирует папку по маске `_man_*.xlsx` и берёт всё, что найдёт (сортировка по имени или по дате).
  > 2. Проверка структуры перед слиянием — сверяет заголовки (строка 5) и диапазон колонок во всех файлах; если где-то расхождение — останавливается и печатает, в каком файле и что не совпадает, а не молча портит данные.
  > 3. Опция сквозной нумерации `№п/п` — флагом включается/выключается, чтобы не гадать каждый раз.
  > 4. Логирование — сколько строк взято из каждого файла, чтобы сразу видеть, что ничего не потерялось (как я делал вручную сейчас).
  >
  > Дальше вопрос — где и как удобнее это запускать:
  > Как вам удобнее запускать объединение манифестов в будущем?Отдельный python-скрипт у себя (запускать вручную на компьютере/сервере)
  > Универсальный скрипт для объединения манифестов (_man_*.xlsx) с проверкой структуры
  > Проверю на исходных файлах, чтобы убедиться, что скрипт даёт тот же результат, что и ручная сборка.
  > Ran 3 commands, read a file
  > Как пользоваться:
  > Нужен Python 3 с библиотекой `openpyxl` (`pip install openpyxl`, если ещё не стоит).
  >
  > ```
  > python merge_manifests.py ./папка_с_манифестами
  > ```
  >
  > По умолчанию берёт все файлы `_man_*.xlsx` в указанной папке, сортирует по алфавиту и кладёт результат в `manifest_combined.xlsx` в той же папке.
  > Полезные опции:
  >
  > - `--renumber` — сделать сквозную нумерацию колонки «№п/п» (сейчас по умолчанию не трогает нумерацию, как обсуждали);
  > - `--order "_man_Shanghai.xlsx,_man_PK.xlsx"` — задать порядок файлов вручную вместо алфавитного;
  > - `--output путь/к/файлу.xlsx` — куда сохранить.
  >
  > Главное, что скрипт делает сам:
  >
  > - находит файлы по маске, не нужно перечислять их руками;
  > - проверяет, что заголовки и диапазон колонок одинаковы во всех файлах — если кто-то поменял структуру, скрипт остановится и покажет, в каком файле и какая именно колонка отличается, вместо того чтобы молча склеить кривые данные;
  > - копирует не только значения, но и весь стиль ячейки (шрифт, заливка, рамки, формат чисел) построчно из исходника — точно то же, что делал я вручную.
  >
  > Я протестировал его на ваших шести файлах — результат идентичен тому, что получилось при ручной сборке (915 строк, то же распределение по файлам), и отдельно проверил, что при подмене одного заголовка скрипт корректно ловит расхождение и не даёт собрать файл.
  > Merge manifests
  > PY
  > GUANG QI DE ER TA 2601W DG MANIFEST.xlsx
  > GUANG QI DE ER TA 2601W DG MANIFEST.xlsx
  > XLSX
  > В этом файле содержится информация о классе опасности грузов. Столбец M класс в столбце J UN. Необходимо сравнить информацию в данном файле с информацией по опасным грузам из ранее полученного собраного сводного файла. Сделай новый столбец в сводном файле. Если найдешь различия, то выдели желтым цветом строки
  > Ran 4 commands
  > Хорошо, формат понятен, и есть контейнеры с несколькими записями (несколько классов/UN в одном контейнере). Проверю уникальность контейнеров и то, как DG-манифест группируется по контейнеру.
  > Ran 9 commands, created a file, read a file
  > Добавил столбец Z «Опасные грузы (сверка с DG-манифестом)» — туда попали официальные UN-номер и класс по каждому контейнеру из `GUANG_QI_DE_ER_TA_2601W_DG_MANIFEST.xlsx` (сопоставление шло по номеру контейнера: столбец E манифеста ↔ ваша колонка «№ контейнера»).
  > Сравнил с уже имевшейся колонкой K «Опасные грузы» и нашёл 23 расхождения — эти строки закрашены жёлтым по всей ширине таблицы (C:Z). Основные типы расхождений:
  >
  > - Перепутаны класс/UN — например, контейнер `SAXU2026718`: в сводном файле "IMO 9 UN 3267 / IMO 8 UN 3077", а по официальному манифесту наоборот — "IMO 9 UN 3077 / IMO 8 UN 3267".
  > - В сводном файле указан опасный груз, которого нет в DG-манифесте — 17 таких контейнеров (например, `CICU1828260`, `NBLU3254400` и другие с UN 3480/3556) — возможно, ошибочно перенесённые данные, либо груз реально не опасный, а в тексте коносамента упомянут по другой причине.
  > - В DG-манифесте больше опасных позиций, чем указано в сводном файле — например, `FCGU2302379` (по манифесту 2 опасных вещества, в сводном — только 1), `DFSU1062572` (по манифесту 6 позиций, в сводном — 2).
  >
  > Строки, где данных об опасном грузе нет ни там, ни там (892 строки) — не трогал, всё чисто.
  > Manifest combined
  > Spreadsheet·XLSX

- 2026-09-06 — пользователь подтвердил: «Два необходимые действия уже учтимы в этом чате» — то есть два действия сайта: (1) объединение нескольких файлов `_man_*.xlsx` в один сводный файл с сохранением форматирования и проверкой структуры, (2) сверка опасных грузов сводного файла с отдельным DG-манифестом по номеру контейнера, с добавлением столбца официальных данных и подсветкой расхождений жёлтым.
- 2026-09-06 — вопрос про размещение сайта (R18i): предложены варианты «локально без сервера», «выложить в интернет», «локальный Python-сервер». Ответ пользователя: «Выложить в интернет».
- 2026-09-06 — вопрос про хостинг: предложены варианты «есть GitHub — GitHub Pages», «нет аккаунта — завести самому/самой», «другой хостинг». Ответ пользователя: «Да, есть GitHub — используй GitHub Pages».
- 2026-09-06 — вопрос про смысл фразы «будем наполнять дополнительными данными» (R04): предложены варианты «речь о новых инструментах позже» и «нужно хранить историю файлов». Ответ пользователя: «Да, речь о новых инструментах позже».
